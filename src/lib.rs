mod components;
import!(logic > pub [*]);

#[cfg(test)]
mod tests
{
    use super::*;

    #[test]
    fn test() {
        let driver = self::browser::open(browser::DriverType::Brave(""));
    }

    #[test]
    fn generate_dir_tree() {}
}
